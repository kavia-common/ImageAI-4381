fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
python-multipart==0.0.9
Pillow==10.4.0
opencv-python-headless==4.10.0.84
# Optional but detected gracefully if missing
torch==2.4.1
# Local library installed editable; in Docker we will install via setup.py at repo root
